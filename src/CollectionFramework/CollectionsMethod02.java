package CollectionFramework;

// 변경불가, 싱글톤 메소드

import java.util.*;

public class CollectionsMethod02 {
//    static Collection unmodifiableCollection(Collection c);
//
//    // List, Set, SortedSet, Map, SortedMap 도 동일하게 unmodifiable 를 앞에 붙이는 메소드가 있다.
//    static NavigableSet unmodifiableNavigableSet(NavigableSet navigableSet);
//    static NavigableMap unmodifiableNavigableMap(NavigableMap navigableMap);

//    static List singletionList(Object o);
//    // singletonSet 이 아닌 singleton이다.
//    static Set singleton(Object o);
//    static Map singletonMap(Object key, Object value);
}
